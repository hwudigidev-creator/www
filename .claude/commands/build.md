# Build - 建置專案

建置 React + Vite 前端專案，產生生產版本到 `dist/` 目錄。

## 執行步驟

1. 切換到 frontend 目錄
2. 執行 `npm run build`
3. 回報建置結果（成功/失敗、檔案大小等）

## 指令

```bash
cd frontend && npm run build
```

## 注意事項

- 確保 `node_modules` 已安裝，若無則先執行 `npm install`
- 建置產出位於 `frontend/dist/`
- 遵循 SPEC.md 的 UTF-8 編碼規範
